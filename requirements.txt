# Analysis pipeline (Analysis/) and the Kaggle Benchmarks arm (kaggle/benchmarks/).
# Pins are the versions present on the machine the analysis is run from.
# FAIRGAME/ carries its own requirements.txt for the simulation framework.

numpy==2.3.5
pandas==2.3.3
pyarrow==22.0.0          # games.parquet
matplotlib==3.10.8
scikit-learn==1.8.0
statsmodels==0.14.6
torch==2.9.1             # LSTM strategy read-out, s01_train_lstm.py
mpmath==1.3.0            # 200-digit arithmetic in s09_egt.py
egttools==0.1.14.2       # evolutionary baseline and invasion diagrams
pytest==9.1.1

# Kaggle Benchmarks arm only
kaggle==2.2.4
kaggle-benchmarks==0.6.1

# The analysis runs on the conda env `egt` (Python 3.12): egttools has no wheel
# for Python 3.14 and its build needs a C++ toolchain this machine lacks.
