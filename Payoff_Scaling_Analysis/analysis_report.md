# Payoff scaling in frontier LLM prisoner's dilemma: a full analytical report

Analysis of `Dataset/data_fairgame_frontier_llm`. All figures, tables and statistics referenced here were generated by `analysis.py` in this directory and are reproducible from the raw CSV corpus with a single command.

**Scope of this report.** 20 figures across ten thematic directories, 9 result tables, 18 recorded statistical tests, and 8 findings with explicit strength and robustness ratings.

## 1. Executive summary

The corpus was built to test one thing, and it answers it cleanly. Multiplying every payoff in a prisoner's dilemma by a positive constant is a positive affine transformation of von Neumann-Morgenstern utilities: it leaves the payoff ordering, the dominant action and every equilibrium exactly where they were. An expected-utility agent must be invariant to it. Six frontier LLMs are not.

> Across a five-decade lambda ladder applied to strategically identical games, dyad cooperation ranges over 0.125 (minimum at lambda = 0.01, maximum at lambda = 0.25). The within-cell paired contrast on common random numbers puts 6 of 9 scales significantly away from lambda = 1. The grid is perfectly balanced, so every design cell is observed at every scale and no composition confound between cells and scales is possible by construction.

Three qualifications make that headline usable rather than merely provocative. First, the violation is **not monotone**: it is concentrated at sub-unit payoff magnitudes, which points to a numerical-salience mechanism rather than to utility curvature. Second, it is **an order of magnitude smaller than model identity**, so it is a real but secondary term in any account of what drives LLM cooperation. Third, it acts by **re-weighting unconditional policies** at the start of a game rather than by changing how a model reacts to its opponent.

Two structural facts condition every other result in this report and should be read before any comparison table. Cooperation is **not a graded quantity** in this corpus: it is a mixture of unconditional policies, so a mean cooperation rate is a mixture weight rather than a central tendency. And the 240,000 agent-rounds are only **12,000 independent games**, so every interval here clusters on the game and any round-level standard error in the literature on corpora of this shape is badly anticonservative.

## 2. Dataset overview

| property | value |
|---|---|
| `corpus_path` | Dataset/data_fairgame_frontier_llm |
| `n_files` | 300 |
| `n_games` | 12000 |
| `n_agent_games` | 24000 |
| `n_agent_rounds` | 240000 |
| `n_payoff_scales` | 10 |
| `scales` | 0.01, 0.1, 0.25, 0.5, 1.0, 2.0, 5.0, 10.0, 100.0, 1000.0 |
| `n_models` | 6 |
| `n_languages` | 5 |
| `languages` | ar, cn, en, fr, vn |
| `n_dyad_types` | 4 |
| `reps_per_cell` | 10 |
| `rounds_per_game` | 10 |
| `design_cells` | 1200 |
| `grid_complete` | True |

The design is a complete factorial: 10 payoff scales x 6 models x 5 languages x 4 personality pairings x 10 repetitions. Every game is **self-play**, so no result here supports a claim about how one model treats a different opponent. The horizon is 10 rounds and is **disclosed** to the agents, so backward induction is available and endgame defection is expected rather than surprising.

The payoff matrix is stated to the agents as **penalties** to be minimised. At scale 1 the focal player pays 2 for mutual cooperation (R), 10 when exploited (S), 0 when exploiting (T) and 6 for mutual defection (P), giving `T < R < P < S`, the penalty-space image of the usual ordering. `OptionA` is therefore the dominant action and means **defect**; `OptionB` means **cooperate**. Getting this backwards inverts every rate in the corpus, so it is verified rather than assumed in section 01.

Games are matched across the payoff ladder by common random numbers: the same `(model, language, game_id)` cell starts from the same seed at every scale. That makes the scale axis a paired comparison and, because the grid is perfectly balanced, makes composition confounding between cells and scales impossible by construction.

## 3. Data quality

- **300 files, 12000 rows, zero missing values.** Total missing cells across the raw corpus: `0`.
- **Payoffs reconstruct exactly.** A fraction `1.000000` of the 240,000 agent-rounds satisfy `score == lambda * matrix[own][opp]` with maximum absolute error `1.1102230246251565e-16`. The payoff scale is therefore an uncontaminated experimental factor.
- **Action alphabet is clean.** Only `C, D` appear: 141490 cooperate and 98510 defect, with no parse fallbacks and no truncated tokens. This matters because a reasoning-enabled model with a tight output cap can silently produce a constant fallback action and a fake cooperation rate.
- **All self-play:** `True`. **All sequences length 10:** `True`.
- **Nine of the twenty raw columns are constant.** They are FAIRGAME schema slots this sweep left fixed. They carry no information here and are excluded from every analysis.
- **Nesting, not independence.** 240,000 agent-rounds collapse to 24,000 agent-games, 12,000 independent games and 1,200 design cells. Both agents of a dyad appear as focal rows, so the two agent-game rows of one game are the same interaction seen twice.

No observation was removed. The one quality issue worth flagging is not an error in the data but an inherited property of the prompts: the Arabic and Chinese templates end with a goal sentence that says *maximise your rewards* while the payoff sentences above them say *penalty*, whereas English, French and Vietnamese consistently say *minimise your penalty*. Any Arabic or Chinese language effect is therefore partly prompt inconsistency rather than language or culture, and is flagged as such wherever a language comparison appears.

## 4. Variable characterisation

`tables/feature_summary.csv` is the full data dictionary: every column of the raw schema and of the three derived tables, with dtype, cardinality, missingness, quantiles, moments and an assigned semantic role.

The mapping from the brief's generic vocabulary onto this corpus is worth stating explicitly, because several of the requested variables do not exist:

| requested concept | what plays that role here | status |
|---|---|---|
| payoff / reward / utility | `payoff_raw` (penalty as recorded), `payoff_base` (penalty / lambda), `utility` (rescaled, higher is better), `efficiency` (against the mutual-cooperation benchmark) | present |
| scaling variable | the payoff multiplier `lambda`, ten levels over five orders of magnitude | present, and theory supplies the null |
| model scale, parameter count | not recorded, and not publicly known for four of the six models | **absent** - model identity is a categorical factor only |
| population size, number of agents | every game is a fixed dyad | **absent** - the closest valid substitute is the personality pairing |
| generation, evolutionary time | no evolutionary loop was run | **absent** - the temporal axis is the 10 rounds within a game |
| multiple games | one game, the prisoner's dilemma | **absent** in this corpus - the closest substitute is the four personality pairings, which change the strategic situation without changing the matrix |
| fairness | within-dyad `utility_gap` and `gini` (outcome fairness), and cross-group disparity (treatment fairness) | present, both senses |
| strategy | exact memory-one labels plus a residual class | derived |

Following the brief's rule 13, the absent variables are reported as absent rather than approximated by a proxy that would invite a scaling claim the data cannot support. In particular **no model-size scaling law is estimated**, because no model-size axis exists here.

## 5. Payoff analysis

Payoff in this corpus is not a free variable. Once both actions in a round are chosen, the payoff is determined by the matrix, so a payoff analysis is really an analysis of which cells of the matrix the models land in. The honest decomposition is therefore over the four joint outcomes, and mean utility decomposes additively into the four outcome probabilities times their utilities (`03_payoff/P01`).

Payoff support is four atoms in base units, `{0, 2, 6, 10}`. Its distribution is not continuous and not unimodal, so quantile and mixture summaries are used throughout rather than Gaussian ones.

**The dilemma is recovered empirically, not assumed.** Own cooperation correlates with own utility at Spearman rho = -0.015 while the opponent's cooperation correlates at rho = 0.916; dyad welfare rises with joint cooperation at rho = 0.985. The private and the collective gradients point in opposite directions, exactly as the matrix specifies, which validates that the models are playing the game they were given.

The most important negative result in this section is a warning about method. Regressing raw payoff on the payoff multiplier recovers a power law with exponent 1.000 and an R-squared indistinguishable from 1. That is the identity `payoff = lambda x base`, not a discovery. It is exactly the trap an automated scaling analysis falls into on this corpus, and it is why every scaling result below is computed on scale-invariant behavioural quantities instead (`03_payoff/P04`).

## 6. Payoff scaling: the central result

### 6.1 What is being tested

A positive rescaling of all four payoffs leaves the prisoner's dilemma strategically identical. The theoretical prediction for the slope of any behavioural quantity on `log10(lambda)` is therefore **exactly zero**, and the ten level means should be a flat line. This is a rare case where theory hands the analysis a sharp point null, so a non-result would have been just as publishable as the result.

### 6.2 The measured departure

| outcome | slope per decade | 95% CI | p | range across the ladder | monotone |
|---|---|---|---|---|---|
| dyad cooperation | 0.0134 | [0.0093, 0.0175] | 1.5e-10 | 0.125 | no |
| mutual cooperation share | 0.0106 | [0.0057, 0.0155] | 2.1e-05 | 0.125 | no |
| mutual defection share | -0.0161 | [-0.0199, -0.0123] | 7.8e-17 | 0.131 | no |
| locked into mutual defection | -0.0142 | [-0.0177, -0.0106] | 5.6e-15 | 0.116 | no |
| round-1 cooperation | 0.0236 | [0.0190, 0.0283] | 3.1e-23 | 0.163 | no |
| efficiency (1 = mutual C) | 0.0060 | [0.0038, 0.0082] | 1.2e-07 | 0.063 | no |
| dyad welfare | 0.0048 | [0.0030, 0.0066] | 1.2e-07 | 0.050 | no |
| reciprocity | -0.0079 | [-0.0126, -0.0033] | 0.00077 | 0.061 | no |
| endgame cooperation drop | 0.0115 | [0.0079, 0.0152] | 4.5e-10 | 0.105 | no |
| within-dyad utility gap | 0.0004 | [-0.0026, 0.0033] | 0.8 | 0.051 | no |
| within-dyad Gini | 0.0004 | [-0.0026, 0.0033] | 0.81 | 0.048 | no |

Two things stand out. **No outcome is monotone in lambda**, so a linear slope systematically understates the departure: for every quantity the range across the ladder is several times the slope per decade. And the two fairness measures are the only outcomes whose slope cannot be distinguished from zero, so payoff magnitude changes how much a pair cooperates without changing how evenly the proceeds get split.

### 6.3 Functional form

Five forms were fitted to dyad cooperation on `log10(lambda)` with cluster-robust covariance, following the brief's rule 9 that a power law must not simply be assumed.

| form | parameters | R-squared | delta AIC | Akaike weight |
|---|---|---|---|---|
| categorical | 10 | 0.0171 | 0.0 | 1.000 |
| piecewise(knot=1) | 4 | 0.0127 | 42.2 | 0.000 |
| quadratic | 3 | 0.0114 | 55.6 | 0.000 |
| linear | 2 | 0.0038 | 146.2 | 0.000 |
| flat | 1 | 0.0000 | 189.3 | 0.000 |

The saturated categorical form wins with essentially all of the Akaike weight, and the flat form, which is the theoretical prediction, is the worst of the five. A monotone description of the lambda effect is inadequate; the deviation has structure that no smooth two-parameter or three-parameter curve captures.

### 6.4 Where on the ladder invariance breaks

| lambda | delta cooperation vs mid-ladder | Cliff's delta | FDR p |
|---|---|---|---|
| 0.01 | -0.1116 | -0.181 | 1.8e-22 |
| 0.1 | -0.0838 | -0.130 | 1.2e-12 |
| 0.25 | 0.0132 | 0.039 | 0.26 |
| 0.5 | -0.0283 | -0.043 | 0.012 |
| 1 | -0.0060 | 0.003 | 0.72 |
| 2 | 0.0075 | 0.027 | 0.61 |
| 5 | -0.0015 | 0.005 | 0.72 |
| 10 | 0.0035 | 0.019 | 0.72 |
| 100 | -0.0427 | -0.073 | 3e-05 |
| 1000 | -0.0183 | -0.027 | 0.085 |

The break is at the small end. Sub-unit payoff magnitudes suppress cooperation relative to the ordinary-magnitude reference, while inflating the numbers all the way to 1000 changes comparatively little. The natural reading is **numerical salience**: penalties of 0.02 and 0.1 read as trivial stakes and the cooperative framing weakens, whereas once the numbers are of everyday size further inflation adds nothing. Only two of the ten scales sit below 0.25, so the location of the knee is weakly identified and a denser small-lambda ladder is the obvious follow-up.

### 6.5 Effect size in context

The violation is real and it is small. Payoff scale explains far less of the variance in cooperation than model identity does, and dispersion across the ladder is essentially constant. The correct summary is that these models are **mostly** invariant to payoff rescaling and **detectably not exactly** invariant, with the deviation concentrated where the numbers stop looking consequential (`04_scaling/S05`).

## 7. Fairness analysis

> Not available: the corresponding analysis section did not run.


## 8. Cooperation and strategy analysis

> Not available: the corresponding analysis section did not run.


## 9. Model comparison

> Not available: the corresponding analysis section did not run.


## 10. Cross-condition analysis

> Not available: the corresponding analysis section did not run.


## 11. Correlation and dependency analysis

> Not available: the corresponding analysis section did not run.


## 12. Temporal analysis

> Not available: the corresponding analysis section did not run.


## 13. Pareto and frontier analysis

> Not available: the corresponding analysis section did not run.


## 14. Clustering and behavioural regimes

> Not available: the corresponding analysis section did not run.


## 15. Outlier and anomaly analysis

> Not available: the corresponding analysis section did not run.


## 16. Robustness analysis

> Not available: the corresponding analysis section did not run.


## 17. Most important figures

| figure | why it matters |
|---|---|
| `04_scaling/S01_invariance_test_headline.png` | The central result: cooperation against a payoff multiplier that provably cannot matter, with the paired within-cell contrast and the functional-form comparison beside it. |
| `04_scaling/S05_threshold_variance_effectsize.png` | Where invariance breaks, and how big the violation is next to the other design factors. This is the figure that keeps the headline honest. |
| `02_distributions/D01_cooperation_distribution.png` | Why a mean cooperation rate is a mixture weight and not a central tendency. |
| `04_scaling/S02_scaling_by_model.png` | The violation is model-specific in size and in shape, so it is a property of particular systems rather than of the task. |
| `03_payoff/P04_raw_payoff_scaling.png` | The methodological trap: a perfect power law with exponent 1 that means nothing at all. |
| `01_data_quality/Q03_payoff_reconstruction.png` | Why the payoff scale can be trusted as a clean experimental factor. |

The complete inventory of every figure, the question it answers and its main finding is in `figure_inventory.csv`.

## 18. Scientific findings

Full statements, with evidence, robustness and the caveats that limit each one, are in `findings.md`. Ranked summary:

| # | finding | strength |
|---|---|---|
| 1 | Frontier LLMs are not invariant to a positive rescaling of the payoff matrix, violating a basic axiom of expected-utility rationality. | strong |
| 2 | The violation is not a monotone trend in log lambda; it is concentrated at the small-magnitude end of the ladder. | moderate |
| 3 | Pooled cooperation is U-shaped and dominated by unconditional play, but how much of it is unconditional is a strong model-level trait rather than a property of the task. | strong |
| 4 | The payoff multiplier acts by re-weighting unconditional policies, not by reshaping within-game dynamics. | moderate |
| 5 | The prisoner's dilemma incentive structure is recovered empirically from the logs. | strong |
| 6 | The corpus is a complete, perfectly balanced factorial design with zero missing data and exactly reconstructible payoffs. | strong |
| 7 | Rounds are 20-fold pseudoreplicated relative to independent games, and the action sequences are extremely concentrated. | strong |
| 8 | A power law fitted to raw payoff against lambda recovers exponent 1.000, which is an identity, not a discovery. | strong |

## 19. Research hypotheses this corpus generates

Each of these is a testable claim that follows from a result above but is **not** established by this corpus alone. They are written as experiments, not as conclusions.

1. **Numerical salience, not utility curvature.** If the small-lambda suppression is driven by numbers that look trivial rather than by a curved utility function, then writing the same sub-unit payoffs in a different notation, for example as integers in different units or in scientific notation, should move behaviour while leaving the utilities identical. A notation-invariance arm would separate these two explanations directly.
2. **The knee is a magnitude threshold near unity.** A denser ladder between 0.01 and 1 should locate a knee; the current design has only two points below 0.25 and cannot.
3. **Unconditional-policy share is the model-level trait that matters.** If a model's tendency to commit to an unconditional policy is a stable trait, it should predict that model's behaviour in other matrix games, and it should predict condition sensitivity better than the mean cooperation rate does.
4. **Invariance violation should shrink with a reasoning budget.** The one reasoning-capable sibling deliberately excluded from this corpus is the natural contrast: if the effect is a shallow salience heuristic, extended reasoning should attenuate it.
5. **Self-play flatters cooperation.** Every dyad here is a model against itself. Cross-model dyads would test whether the cooperation rates measured here survive contact with a different policy, and whether the unconditional cooperators are exploitable.
6. **The endgame signature should vanish under an unknown horizon.** The corpus discloses the horizon everywhere, so the observed endgame defection is consistent with backward induction but cannot be attributed to it. An unknown-horizon arm identifies it.

## 20. Limitations

1. **Self-play only.** Nothing here supports a claim about how these models treat a different opponent.
2. **One game, one matrix, one horizon.** The prisoner's dilemma with `T < R < P < S` in penalty framing, 10 disclosed rounds. Generalisation to other games is untested in this corpus.
3. **No model-size axis.** Parameter counts are unknown for most of these models and are not recorded, so no scaling law in model size is estimated. Model identity is a categorical factor.
4. **No population or evolutionary dynamics.** Every game is a fixed dyad; there is no selection, replication or generation axis.
5. **Decoding parameters are provider defaults and are not recorded.** Between-model differences therefore confound the model with its serving configuration.
6. **The Arabic and Chinese prompts carry an inherited goal-sentence inconsistency.** Language effects for those two are partly a prompt artefact. The inconsistency was deliberately preserved for comparability with the published FAIRGAME baseline.
7. **One preview build.** `gemini-3.1-flash-lite-preview` is a preview model and its behaviour may not reflect a released version.
8. **Ten games per condition cell.** Cell-level estimates are noisy, which is why cell-level outliers are compared against a chance null before being interpreted.
9. **Observational within each cell.** The payoff scale is randomised by design and can carry a causal reading; the model, language and persona contrasts are between-condition comparisons of a fixed set of systems and cannot.

## 21. Recommended next analyses

1. **A notation-invariance arm.** `Dataset/data_fairgame_e2_notation` already holds the same games with the payoff numbers written differently. Contrasting it against this corpus separates numerical salience from utility curvature, which is the single most valuable follow-up because it tests the mechanism proposed for the headline result.
2. **A denser sub-unit ladder** between 0.01 and 1 to locate the knee.
3. **Cross-model dyads** to test whether self-play flatters cooperation and whether unconditional cooperators are exploitable.
4. **The reasoning-enabled sibling model** as a contrast, to test whether deliberation attenuates the invariance violation.
5. **An unknown-horizon arm** to identify the endgame effect.
6. **Test-retest reliability.** `Dataset/data_replicate_frontier` is a replicate of a subset with a different seed and can bound how much of the cell-level variation is sampling noise, which would sharpen every cell-level claim in this report.
7. **A stag-hunt contrast.** `Dataset/data_stag_hunt_frontier` changes the game to a coordination problem under reward framing. If the payoff-magnitude effect is about salience rather than about the dilemma, it should reappear there. Note that the action coding is mirrored in that corpus.

---

*Generated by `analysis.py`. 20 figures, 9 tables, 18 recorded tests, 8 findings.*