# --- secrets: KHÔNG BAO GIỜ commit -------------------------------------------
.env
.env.*
!.env.example
*.key
kaggle.json
access_token

# --- kaggle-benchmarks: artefact chạy cục bộ ---------------------------------
# SDK ghi <task>.task.json / <task>-run_id_*.run.json vào cwd — có thể là repo root.
kaggle/benchmarks/results/
kaggle/benchmarks/checkpoints/
*.run.json
*.task.json

# --- output khi chạy thử FAIRGAME tại máy ------------------------------------
FAIRGAME/resources/results/
pd_results/
pd_results.zip

# --- python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.venv/
venv/

# --- LaTeX: artefact biên dịch --------------------------------------------
# Giữ .aux và .bbl: main.tex đọc appendix.aux qua xr, và Overleaf cần .bbl.
*.log
*.blg
*.out
*.toc
*.bcf
*.run.xml
*.synctex.gz
*.fls
*.fdb_latexmk
!Analysis/scaling/*.log

# --- riêng tư: KHÔNG public -------------------------------------------------
# Thư từ toà soạn, ghi chú nội bộ, giấy tờ cá nhân.
private/

# --- OS ----------------------------------------------------------------------
.DS_Store
Thumbs.db
